{{
  config(
    materialized = "view",
    alias = "cm50_po_lines_all",
    schema='EBS'
  )
}}

SELECT
POL_PO_LINE_ID::NUMBER AS POL_PO_LINE_ID,
TO_TIMESTAMP_NTZ(POL_LAST_UPDATE_DATE::string) AS POL_LAST_UPDATE_DATE,
POL_LAST_UPDATED_BY::NUMBER AS POL_LAST_UPDATED_BY,
POL_PO_HEADER_ID::NUMBER AS POL_PO_HEADER_ID,
POL_LINE_TYPE_ID::NUMBER AS POL_LINE_TYPE_ID,
POL_LINE_NUM::NUMBER AS POL_LINE_NUM,
POL_LAST_UPDATE_LOGIN::NUMBER AS POL_LAST_UPDATE_LOGIN,
TO_TIMESTAMP_NTZ(POL_CREATION_DATE::string) AS POL_CREATION_DATE,
POL_CREATED_BY::NUMBER AS POL_CREATED_BY,
POL_ITEM_ID::NUMBER AS POL_ITEM_ID,
POL_ITEM_REVISION::VARCHAR(32) AS POL_ITEM_REVISION,
POL_CATEGORY_ID::NUMBER AS POL_CATEGORY_ID,
POL_ITEM_DESCRIPTION::VARCHAR(32) AS POL_ITEM_DESCRIPTION,
POL_UNIT_MEAS_LOOKUP_CODE::VARCHAR(32) AS POL_UNIT_MEAS_LOOKUP_CODE,
POL_QUANTITY_COMMITTED::NUMBER AS POL_QUANTITY_COMMITTED,
POL_ALLOW_PRICE_OVERRIDE_FLAG::VARCHAR(32) AS POL_ALLOW_PRICE_OVERRIDE_FLAG,
POL_LIST_PRICE_PER_UNIT::NUMBER AS POL_LIST_PRICE_PER_UNIT,
POL_UNIT_PRICE::NUMBER AS POL_UNIT_PRICE,
POL_QUANTITY::NUMBER AS POL_QUANTITY,
POL_NOTE_TO_VENDOR::VARCHAR(32) AS POL_NOTE_TO_VENDOR,
POL_FROM_HEADER_ID::NUMBER AS POL_FROM_HEADER_ID,
POL_FROM_LINE_ID::NUMBER AS POL_FROM_LINE_ID,
POL_QTY_RCV_TOLERANCE::NUMBER AS POL_QTY_RCV_TOLERANCE,
POL_OVER_TOLERANCE_ERROR_FLAG::VARCHAR(32) AS POL_OVER_TOLERANCE_ERROR_FLAG,
POL_MARKET_PRICE::NUMBER AS POL_MARKET_PRICE,
POL_UNORDERED_FLAG::VARCHAR(32) AS POL_UNORDERED_FLAG,
POL_CLOSED_FLAG::VARCHAR(32) AS POL_CLOSED_FLAG,
POL_USER_HOLD_FLAG::VARCHAR(32) AS POL_USER_HOLD_FLAG,
POL_CANCEL_FLAG::VARCHAR(32) AS POL_CANCEL_FLAG,
POL_CANCELLED_BY::NUMBER AS POL_CANCELLED_BY,
TO_TIMESTAMP_NTZ(POL_CANCEL_DATE::string) AS POL_CANCEL_DATE,
POL_CANCEL_REASON::VARCHAR(32) AS POL_CANCEL_REASON,
POL_FIRM_STATUS_LOOKUP_CODE::VARCHAR(32) AS POL_FIRM_STATUS_LOOKUP_CODE,
POL_VENDOR_PRODUCT_NUM::VARCHAR(32) AS POL_VENDOR_PRODUCT_NUM,
POL_TAXABLE_FLAG::VARCHAR(32) AS POL_TAXABLE_FLAG,
POL_TAX_NAME::VARCHAR(32) AS POL_TAX_NAME,
POL_TYPE_1099::VARCHAR(32) AS POL_TYPE_1099,
POL_CAPITAL_EXPENSE_FLAG::VARCHAR(32) AS POL_CAPITAL_EXPENSE_FLAG,
POL_NEGOTIATED_FLAG::VARCHAR(32) AS POL_NEGOTIATED_FLAG,
POL_ATTRIBUTE1::VARCHAR(32) AS POL_ATTRIBUTE1,
POL_ATTRIBUTE2::VARCHAR(32) AS POL_ATTRIBUTE2,
POL_ATTRIBUTE3::VARCHAR(32) AS POL_ATTRIBUTE3,
POL_ATTRIBUTE4::VARCHAR(32) AS POL_ATTRIBUTE4,
POL_ATTRIBUTE5::VARCHAR(32) AS POL_ATTRIBUTE5,
POL_ATTRIBUTE6::VARCHAR(32) AS POL_ATTRIBUTE6,
POL_ATTRIBUTE7::VARCHAR(32) AS POL_ATTRIBUTE7,
POL_PRICE_TYPE_LOOKUP_CODE::VARCHAR(32) AS POL_PRICE_TYPE_LOOKUP_CODE,
POL_CLOSED_CODE::VARCHAR(32) AS POL_CLOSED_CODE,
POL_PRICE_BREAK_LOOKUP_CODE::VARCHAR(32) AS POL_PRICE_BREAK_LOOKUP_CODE,
POL_REQUEST_ID::NUMBER AS POL_REQUEST_ID,
POL_PROGRAM_APPLICATION_ID::NUMBER AS POL_PROGRAM_APPLICATION_ID,
POL_PROGRAM_ID::NUMBER AS POL_PROGRAM_ID,
TO_TIMESTAMP_NTZ(POL_PROGRAM_UPDATE_DATE::string) AS POL_PROGRAM_UPDATE_DATE,
TO_TIMESTAMP_NTZ(POL_CLOSED_DATE::string) AS POL_CLOSED_DATE,
POL_CLOSED_REASON::VARCHAR(32) AS POL_CLOSED_REASON,
POL_CLOSED_BY::NUMBER AS POL_CLOSED_BY,
POL_ORG_ID::NUMBER AS POL_ORG_ID,
TO_TIMESTAMP_NTZ(POL_EXPIRATION_DATE::string) AS POL_EXPIRATION_DATE,
POL_TAX_CODE_ID::NUMBER AS POL_TAX_CODE_ID,
TO_TIMESTAMP_NTZ(POL_RETROACTIVE_DATE::string) AS POL_RETROACTIVE_DATE,
POL_CONTRACT_ID::NUMBER AS POL_CONTRACT_ID,
POL_ORDER_TYPE_LOOKUP_CODE::VARCHAR(32) AS POL_ORDER_TYPE_LOOKUP_CODE,
POL_PURCHASE_BASIS::VARCHAR(32) AS POL_PURCHASE_BASIS,
POL_MATCHING_BASIS::VARCHAR(32) AS POL_MATCHING_BASIS,
POL_BASE_UNIT_PRICE::NUMBER AS POL_BASE_UNIT_PRICE,
POL_MANUAL_PRICE_CHANGE_FLAG::VARCHAR(32) AS POL_MANUAL_PRICE_CHANGE_FLAG
  
FROM {{ref('prep_cm50_po_lines_all')}}
QUALIFY ROW_NUMBER() OVER (PARTITION BY POL_PO_LINE_ID ORDER BY TO_TIMESTAMP_NTZ(POL_LAST_UPDATE_DATE) DESC) = 1
